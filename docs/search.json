---
layout: ~
---
[
  {%- assign pages = site.pages | where_exp:"item","item.title" -%}
  {% for page in pages %}
    {
      "title"    : "{{ page.title | escape }}",
      "category" : "{{ page.category }}",
      "content"	 : "{{ page.content | strip_html | strip_newlines | normalize_whitespace | escape | remove: "\"}}",
      "url"      : "{{ page.baseurl }}{{ page.url }}"
    }{% unless forloop.last %},{% endunless %}
  {%- endfor %}
]
